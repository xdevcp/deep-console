<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="app"/>
    <springProperty scope="context" name="logPath" source="all.logs.path" defaultValue="${user.home}/.${appName}/logs"/>

    <property name="LOG_HOME" value="${logPath}"/>
    <property name="LogPattern"
              value="%-12(%d{yyyy-MM-dd HH:mm:ss.SSS}) %-5level %thread %c [%L] %msg%n"/>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <appender name="stdoutAppender" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>UTF-8</charset>
            <pattern>${LogPattern}</pattern>
        </encoder>
    </appender>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="rootFile"
              class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/all.log</file>
        <append>true</append>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/all.log.%d{yyyy-MM-dd}.%i</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <MaxHistory>15</MaxHistory>
            <totalSizeCap>512MB</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
        <encoder>
            <Pattern>%date %level %msg%n%n</Pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <root>
        <level value="INFO"/>
        <appender-ref ref="rootFile"/>
    </root>

    <springProfile name="standalone">

        <logger name="org.springframework">
            <appender-ref ref="stdoutAppender"/>
            <level value="INFO"/>
        </logger>

        <logger name="org.apache.catalina.util.LifecycleBase">
            <appender-ref ref="stdoutAppender"/>
            <level value="ERROR"/>
        </logger>

        <logger name="org.apache.coyote.http11.Http11NioProtocol">
            <appender-ref ref="stdoutAppender"/>
            <level value="WARN"/>
        </logger>

        <logger name="org.apache.tomcat.util.net.NioSelectorPool">
            <appender-ref ref="stdoutAppender"/>
            <level value="WARN"/>
        </logger>

    </springProfile>

    <logger name="cc.devcp.project.core" additivity="false">
        <level value="DEBUG"/>
        <appender-ref ref="stdoutAppender"/>
    </logger>

<!--    <logger name="cc.devcp.project">-->
<!--        <appender-ref ref="stdoutAppender"/>-->
<!--        <level value="INFO"/>-->
<!--    </logger>-->

    <include resource="META-INF/logback/provider-included.xml" optional="true"/>
    <include resource="META-INF/logback/auth-included.xml" optional="true"/>
    <include resource="META-INF/logback/upload-included.xml" optional="true"/>
    <include resource="META-INF/logback/console-included.xml" optional="true"/>

</configuration>
