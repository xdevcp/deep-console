package cc.devcp.project.config.server.model;

/**
 * auth type
 *
 * @author Nacos
 */
public enum AuthType {
    /**
     * auth type
     */
    GROUP,
    GROUP_DATAID,
    TENANT_GROUP,
    TENANT
}
